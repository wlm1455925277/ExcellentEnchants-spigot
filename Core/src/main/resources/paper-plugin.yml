main: su.nightexpress.excellentenchants.EnchantsPlugin
bootstrapper: su.nightexpress.excellentenchants.bridge.paper.PaperEnchantsBootstrap
name: ExcellentEnchants
version: '${project.version}'
description: 大量新增附魔，并与服务器机制正确/深度集成！
api-version: '1.21'
#loader: io.papermc.testplugin.TestPluginLoader
dependencies:
  bootstrap:
    nightcore:
      load: BEFORE
      required: true
  server:
    nightcore:
      load: BEFORE
      required: true
    ProtocolLib:
      load: BEFORE
      required: false
    packetevents:
      load: BEFORE
      required: false
    PlaceholderAPI:
      required: false
    MythicMobs:
      required: false